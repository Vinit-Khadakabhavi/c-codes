//using iterative method

#include <stdio.h>

int main()
{
    int fact=1,i,num;
    printf("enter a number : ");
    scanf("%d",&num);
    for(i=num;i>=1;i--)
    {
        fact = fact*i;
    }
    printf("factorial of a number is %d",fact);

    return 0;
}
